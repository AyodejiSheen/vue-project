const actions = {
    //we dispatch an action
    // when we need to perform more than a mutation
    // increaseAction: (context, payload) => {
    //     context.commit( {type: "increaseCount", number: payload.number} );
    // }

    // taskcompleted: (context, payload) => {
    //     context.commit()
    // }

};

export default actions; 